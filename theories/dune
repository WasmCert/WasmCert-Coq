(coq.theory
  (name Wasm)
  (package Wasm)
  (flags -w -notation-overridden -w -abstract-large-number -w -extraction-reserved-identifier))

;(coq.extraction
;	(prelude extraction)
;	(extracted_modules extract))
